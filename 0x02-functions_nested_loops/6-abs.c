#include "main.h"

/**
 * _abs - main entry point
 * description: function generates absolute value
 * @i: integer
 * Return: int
 */

int _abs(int i)
{
if (i < 0)
{
	return (i * -1);
}

else
{
	return (i);
}
}
