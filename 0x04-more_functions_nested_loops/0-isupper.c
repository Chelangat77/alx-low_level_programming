#include "main.h"

/**
 * _isupper - main entry point
 * @c: c is integer
 * Return: return 1 if c is uppercase else return 0
 * Description: fuction identifies uppercase letters
 */

int _isupper(int c)
{
	for (c = 65; c <= 90; c++)
	{
		return (1);
		c++;
	}
return (0);
}
