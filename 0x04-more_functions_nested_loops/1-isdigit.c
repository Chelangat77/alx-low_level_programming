#include "main.h"

/**
 * _isdigit - main entry point
 * @c: c is the integer
 * Return: return 1 if true else return 0
 * Description: function identifies digits 0 to 9
 */

int _isdigit(int c)
{
int a = 0;
	for (a = 0; a <= 9; a++)
	{
		return (1);
	}
return (0);
}

